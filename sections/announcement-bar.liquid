{% comment %}
  La barra de anuncios ahora está integrada en la sección "Header".
  Esta sección ya no renderiza nada — configura los mensajes desde Header > "Barra de Anuncios".
{% endcomment %}

{% schema %}
{
  "name": "Announcement Bar (legacy)",
  "settings": [
    {
      "type": "paragraph",
      "content": "⚠️ Esta sección ya no se usa. La barra de anuncios ahora está integrada en el Header. Ve a Header → 'Barra de Anuncios' para configurarla."
    }
  ],
  "presets": [{ "name": "Announcement Bar", "category": "Header" }]
}
{% endschema %}
